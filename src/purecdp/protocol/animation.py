# DO NOT EDIT — generated by purecdp._generator from Animation (devtools-protocol 3bd8f01934b5)
'''CDP domain: Animation.

**EXPERIMENTAL**
'''

from __future__ import annotations

import enum
import typing
from dataclasses import dataclass

from .._shared import T_JSON_DICT, event_class

from . import dom, runtime


@dataclass(slots=True)
class Animation:
    '''Animation instance.'''
    #: `Animation`'s id.
    id: str
    #: `Animation`'s name.
    name: str
    #: `Animation`'s internal paused state.
    paused_state: bool
    #: `Animation`'s play state.
    play_state: str
    #: `Animation`'s playback rate.
    playback_rate: float
    #: `Animation`'s start time. Milliseconds for time based animations and percentage [0 - 100] for scroll driven animations (i.e. when viewOrScrollTimeline exists).
    start_time: float
    #: `Animation`'s current time.
    current_time: float
    #: Animation type of `Animation`. Allowed values: CSSTransition, CSSAnimation, WebAnimation
    type: str
    #: *(Optional)* `Animation`'s source animation node.
    source: AnimationEffect | None = None
    #: *(Optional)* A unique ID for `Animation` representing the sources that triggered this CSS animation/transition.
    css_id: str | None = None
    #: *(Optional)* View or scroll timeline
    view_or_scroll_timeline: ViewOrScrollTimeline | None = None

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['id'] = self.id
        json['name'] = self.name
        json['pausedState'] = self.paused_state
        json['playState'] = self.play_state
        json['playbackRate'] = self.playback_rate
        json['startTime'] = self.start_time
        json['currentTime'] = self.current_time
        json['type'] = self.type
        if self.source is not None:
            json['source'] = self.source.to_json()
        if self.css_id is not None:
            json['cssId'] = self.css_id
        if self.view_or_scroll_timeline is not None:
            json['viewOrScrollTimeline'] = self.view_or_scroll_timeline.to_json()
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> Animation:
        return cls(
            id=json['id'],
            name=json['name'],
            paused_state=json['pausedState'],
            play_state=json['playState'],
            playback_rate=float(json['playbackRate']),
            start_time=float(json['startTime']),
            current_time=float(json['currentTime']),
            type=json['type'],
            source=AnimationEffect.from_json(json['source']) if json.get('source') is not None else None,
            css_id=json['cssId'] if json.get('cssId') is not None else None,
            view_or_scroll_timeline=ViewOrScrollTimeline.from_json(json['viewOrScrollTimeline']) if json.get('viewOrScrollTimeline') is not None else None,
        )


@dataclass(slots=True)
class ViewOrScrollTimeline:
    '''Timeline instance'''
    #: Orientation of the scroll
    axis: dom.ScrollOrientation
    #: *(Optional)* Scroll container node
    source_node_id: dom.BackendNodeId | None = None
    #: *(Optional)* Represents the starting scroll position of the timeline as a length offset in pixels from scroll origin.
    start_offset: float | None = None
    #: *(Optional)* Represents the ending scroll position of the timeline as a length offset in pixels from scroll origin.
    end_offset: float | None = None
    #: *(Optional)* The element whose principal box's visibility in the scrollport defined the progress of the timeline. Does not exist for animations with ScrollTimeline
    subject_node_id: dom.BackendNodeId | None = None

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['axis'] = self.axis.to_json()
        if self.source_node_id is not None:
            json['sourceNodeId'] = self.source_node_id.to_json()
        if self.start_offset is not None:
            json['startOffset'] = self.start_offset
        if self.end_offset is not None:
            json['endOffset'] = self.end_offset
        if self.subject_node_id is not None:
            json['subjectNodeId'] = self.subject_node_id.to_json()
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> ViewOrScrollTimeline:
        return cls(
            axis=dom.ScrollOrientation.from_json(json['axis']),
            source_node_id=dom.BackendNodeId.from_json(json['sourceNodeId']) if json.get('sourceNodeId') is not None else None,
            start_offset=float(json['startOffset']) if json.get('startOffset') is not None else None,
            end_offset=float(json['endOffset']) if json.get('endOffset') is not None else None,
            subject_node_id=dom.BackendNodeId.from_json(json['subjectNodeId']) if json.get('subjectNodeId') is not None else None,
        )


@dataclass(slots=True)
class AnimationEffect:
    '''AnimationEffect instance'''
    #: `AnimationEffect`'s delay.
    delay: float
    #: `AnimationEffect`'s end delay.
    end_delay: float
    #: `AnimationEffect`'s iteration start.
    iteration_start: float
    #: `AnimationEffect`'s iteration duration. Milliseconds for time based animations and percentage [0 - 100] for scroll driven animations (i.e. when viewOrScrollTimeline exists).
    duration: float
    #: `AnimationEffect`'s playback direction.
    direction: str
    #: `AnimationEffect`'s fill mode.
    fill: str
    #: `AnimationEffect`'s timing function.
    easing: str
    #: *(Optional)* `AnimationEffect`'s iterations. Omitted if the value is infinite.
    iterations: float | None = None
    #: *(Optional)* `AnimationEffect`'s target node.
    backend_node_id: dom.BackendNodeId | None = None
    #: *(Optional)* `AnimationEffect`'s keyframes.
    keyframes_rule: KeyframesRule | None = None

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['delay'] = self.delay
        json['endDelay'] = self.end_delay
        json['iterationStart'] = self.iteration_start
        json['duration'] = self.duration
        json['direction'] = self.direction
        json['fill'] = self.fill
        json['easing'] = self.easing
        if self.iterations is not None:
            json['iterations'] = self.iterations
        if self.backend_node_id is not None:
            json['backendNodeId'] = self.backend_node_id.to_json()
        if self.keyframes_rule is not None:
            json['keyframesRule'] = self.keyframes_rule.to_json()
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> AnimationEffect:
        return cls(
            delay=float(json['delay']),
            end_delay=float(json['endDelay']),
            iteration_start=float(json['iterationStart']),
            duration=float(json['duration']),
            direction=json['direction'],
            fill=json['fill'],
            easing=json['easing'],
            iterations=float(json['iterations']) if json.get('iterations') is not None else None,
            backend_node_id=dom.BackendNodeId.from_json(json['backendNodeId']) if json.get('backendNodeId') is not None else None,
            keyframes_rule=KeyframesRule.from_json(json['keyframesRule']) if json.get('keyframesRule') is not None else None,
        )


@dataclass(slots=True)
class KeyframesRule:
    '''Keyframes Rule'''
    #: List of animation keyframes.
    keyframes: list[KeyframeStyle]
    #: *(Optional)* CSS keyframed animation's name.
    name: str | None = None

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['keyframes'] = [i.to_json() for i in self.keyframes]
        if self.name is not None:
            json['name'] = self.name
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> KeyframesRule:
        return cls(
            keyframes=[KeyframeStyle.from_json(i) for i in json['keyframes']],
            name=json['name'] if json.get('name') is not None else None,
        )


@dataclass(slots=True)
class KeyframeStyle:
    '''Keyframe Style'''
    #: Keyframe's time offset.
    offset: str
    #: `AnimationEffect`'s timing function.
    easing: str

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['offset'] = self.offset
        json['easing'] = self.easing
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> KeyframeStyle:
        return cls(
            offset=json['offset'],
            easing=json['easing'],
        )


def disable() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Disables animation domain notifications.'''
    yield {'method': 'Animation.disable'}


def enable() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Enables animation domain notifications.'''
    yield {'method': 'Animation.enable'}


def get_current_time(id: str) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, float]:
    '''Returns the current time of the an animation.

    :param id: Id of animation.
    :returns: Current time of the page.
    '''
    params: T_JSON_DICT = {}
    params['id'] = id
    json = yield {'method': 'Animation.getCurrentTime', 'params': params}
    return float(json['currentTime'])


def get_playback_rate() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, float]:
    '''Gets the playback rate of the document timeline.

    :returns: Playback rate for animations on page.
    '''
    json = yield {'method': 'Animation.getPlaybackRate'}
    return float(json['playbackRate'])


def release_animations(animations: list[str]) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Releases a set of animations to no longer be manipulated.

    :param animations: List of animation ids to seek.
    '''
    params: T_JSON_DICT = {}
    params['animations'] = list(animations)
    yield {'method': 'Animation.releaseAnimations', 'params': params}


def resolve_animation(
        animation_id: str,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, runtime.RemoteObject]:
    '''Gets the remote object of the Animation.

    :param animation_id: Animation id.
    :returns: Corresponding remote object.
    '''
    params: T_JSON_DICT = {}
    params['animationId'] = animation_id
    json = yield {'method': 'Animation.resolveAnimation', 'params': params}
    return runtime.RemoteObject.from_json(json['remoteObject'])


def seek_animations(
        animations: list[str],
        current_time: float,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Seek a set of animations to a particular time within each animation.

    :param animations: List of animation ids to seek.
    :param current_time: Set the current time of each animation.
    '''
    params: T_JSON_DICT = {}
    params['animations'] = list(animations)
    params['currentTime'] = current_time
    yield {'method': 'Animation.seekAnimations', 'params': params}


def set_paused(
        animations: list[str],
        paused: bool,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Sets the paused state of a set of animations.

    :param animations: Animations to set the pause state of.
    :param paused: Paused state to set to.
    '''
    params: T_JSON_DICT = {}
    params['animations'] = list(animations)
    params['paused'] = paused
    yield {'method': 'Animation.setPaused', 'params': params}


def set_playback_rate(playback_rate: float) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Sets the playback rate of the document timeline.

    :param playback_rate: Playback rate for animations on page
    '''
    params: T_JSON_DICT = {}
    params['playbackRate'] = playback_rate
    yield {'method': 'Animation.setPlaybackRate', 'params': params}


def set_timing(
        animation_id: str,
        duration: float,
        delay: float,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Sets the timing of an animation node.

    :param animation_id: Animation id.
    :param duration: Duration of the animation.
    :param delay: Delay of the animation.
    '''
    params: T_JSON_DICT = {}
    params['animationId'] = animation_id
    params['duration'] = duration
    params['delay'] = delay
    yield {'method': 'Animation.setTiming', 'params': params}


@event_class('Animation.animationCanceled')
@dataclass(slots=True)
class AnimationCanceled:
    '''Event for when an animation has been cancelled.'''
    #: Id of the animation that was cancelled.
    id: str

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> AnimationCanceled:
        return cls(
            id=json['id'],
        )


@event_class('Animation.animationCreated')
@dataclass(slots=True)
class AnimationCreated:
    '''Event for each animation that has been created.'''
    #: Id of the animation that was created.
    id: str

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> AnimationCreated:
        return cls(
            id=json['id'],
        )


@event_class('Animation.animationStarted')
@dataclass(slots=True)
class AnimationStarted:
    '''Event for animation that has been started.'''
    #: Animation that was started.
    animation: Animation

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> AnimationStarted:
        return cls(
            animation=Animation.from_json(json['animation']),
        )


@event_class('Animation.animationUpdated')
@dataclass(slots=True)
class AnimationUpdated:
    '''Event for animation that has been updated.'''
    #: Animation that was updated.
    animation: Animation

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> AnimationUpdated:
        return cls(
            animation=Animation.from_json(json['animation']),
        )
