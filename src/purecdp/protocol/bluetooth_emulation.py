# DO NOT EDIT — generated by purecdp._generator from BluetoothEmulation (devtools-protocol 3bd8f01934b5)
'''CDP domain: BluetoothEmulation.

This domain allows configuring virtual Bluetooth devices to test
the web-bluetooth API.

**EXPERIMENTAL**
'''

from __future__ import annotations

import enum
import typing
from dataclasses import dataclass

from .._shared import T_JSON_DICT, event_class


class CentralState(enum.StrEnum):
    '''Indicates the various states of Central.'''
    ABSENT = 'absent'
    POWERED_OFF = 'powered-off'
    POWERED_ON = 'powered-on'

    def to_json(self) -> str:
        return self.value

    @classmethod
    def from_json(cls, json: str) -> CentralState:
        return cls(json)


class GATTOperationType(enum.StrEnum):
    '''Indicates the various types of GATT event.'''
    CONNECTION = 'connection'
    DISCOVERY = 'discovery'

    def to_json(self) -> str:
        return self.value

    @classmethod
    def from_json(cls, json: str) -> GATTOperationType:
        return cls(json)


class CharacteristicWriteType(enum.StrEnum):
    '''Indicates the various types of characteristic write.'''
    WRITE_DEFAULT_DEPRECATED = 'write-default-deprecated'
    WRITE_WITH_RESPONSE = 'write-with-response'
    WRITE_WITHOUT_RESPONSE = 'write-without-response'

    def to_json(self) -> str:
        return self.value

    @classmethod
    def from_json(cls, json: str) -> CharacteristicWriteType:
        return cls(json)


class CharacteristicOperationType(enum.StrEnum):
    '''Indicates the various types of characteristic operation.'''
    READ = 'read'
    WRITE = 'write'
    SUBSCRIBE_TO_NOTIFICATIONS = 'subscribe-to-notifications'
    UNSUBSCRIBE_FROM_NOTIFICATIONS = 'unsubscribe-from-notifications'

    def to_json(self) -> str:
        return self.value

    @classmethod
    def from_json(cls, json: str) -> CharacteristicOperationType:
        return cls(json)


class DescriptorOperationType(enum.StrEnum):
    '''Indicates the various types of descriptor operation.'''
    READ = 'read'
    WRITE = 'write'

    def to_json(self) -> str:
        return self.value

    @classmethod
    def from_json(cls, json: str) -> DescriptorOperationType:
        return cls(json)


@dataclass(slots=True)
class ManufacturerData:
    '''Stores the manufacturer data'''
    #: Company identifier https://bitbucket.org/bluetooth-SIG/public/src/main/assigned_numbers/company_identifiers/company_identifiers.yaml https://usb.org/developers
    key: int
    #: Manufacturer-specific data (Encoded as a base64 string when passed over JSON)
    data: str

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['key'] = self.key
        json['data'] = self.data
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> ManufacturerData:
        return cls(
            key=json['key'],
            data=json['data'],
        )


@dataclass(slots=True)
class ScanRecord:
    '''Stores the byte data of the advertisement packet sent by a Bluetooth device.'''
    #: *(Optional)*
    name: str | None = None
    #: *(Optional)*
    uuids: list[str] | None = None
    #: *(Optional)* Stores the external appearance description of the device.
    appearance: int | None = None
    #: *(Optional)* Stores the transmission power of a broadcasting device.
    tx_power: int | None = None
    #: *(Optional)* Key is the company identifier and the value is an array of bytes of manufacturer specific data.
    manufacturer_data: list[ManufacturerData] | None = None

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        if self.name is not None:
            json['name'] = self.name
        if self.uuids is not None:
            json['uuids'] = list(self.uuids)
        if self.appearance is not None:
            json['appearance'] = self.appearance
        if self.tx_power is not None:
            json['txPower'] = self.tx_power
        if self.manufacturer_data is not None:
            json['manufacturerData'] = [i.to_json() for i in self.manufacturer_data]
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> ScanRecord:
        return cls(
            name=json['name'] if json.get('name') is not None else None,
            uuids=list(json['uuids']) if json.get('uuids') is not None else None,
            appearance=json['appearance'] if json.get('appearance') is not None else None,
            tx_power=json['txPower'] if json.get('txPower') is not None else None,
            manufacturer_data=[ManufacturerData.from_json(i) for i in json['manufacturerData']] if json.get('manufacturerData') is not None else None,
        )


@dataclass(slots=True)
class ScanEntry:
    '''Stores the advertisement packet information that is sent by a Bluetooth device.'''
    device_address: str
    rssi: int
    scan_record: ScanRecord

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['deviceAddress'] = self.device_address
        json['rssi'] = self.rssi
        json['scanRecord'] = self.scan_record.to_json()
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> ScanEntry:
        return cls(
            device_address=json['deviceAddress'],
            rssi=json['rssi'],
            scan_record=ScanRecord.from_json(json['scanRecord']),
        )


@dataclass(slots=True)
class CharacteristicProperties:
    '''Describes the properties of a characteristic. This follows Bluetooth Core
    Specification BT 4.2 Vol 3 Part G 3.3.1. Characteristic Properties.
    '''
    #: *(Optional)*
    broadcast: bool | None = None
    #: *(Optional)*
    read: bool | None = None
    #: *(Optional)*
    write_without_response: bool | None = None
    #: *(Optional)*
    write: bool | None = None
    #: *(Optional)*
    notify: bool | None = None
    #: *(Optional)*
    indicate: bool | None = None
    #: *(Optional)*
    authenticated_signed_writes: bool | None = None
    #: *(Optional)*
    extended_properties: bool | None = None

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        if self.broadcast is not None:
            json['broadcast'] = self.broadcast
        if self.read is not None:
            json['read'] = self.read
        if self.write_without_response is not None:
            json['writeWithoutResponse'] = self.write_without_response
        if self.write is not None:
            json['write'] = self.write
        if self.notify is not None:
            json['notify'] = self.notify
        if self.indicate is not None:
            json['indicate'] = self.indicate
        if self.authenticated_signed_writes is not None:
            json['authenticatedSignedWrites'] = self.authenticated_signed_writes
        if self.extended_properties is not None:
            json['extendedProperties'] = self.extended_properties
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> CharacteristicProperties:
        return cls(
            broadcast=json['broadcast'] if json.get('broadcast') is not None else None,
            read=json['read'] if json.get('read') is not None else None,
            write_without_response=json['writeWithoutResponse'] if json.get('writeWithoutResponse') is not None else None,
            write=json['write'] if json.get('write') is not None else None,
            notify=json['notify'] if json.get('notify') is not None else None,
            indicate=json['indicate'] if json.get('indicate') is not None else None,
            authenticated_signed_writes=json['authenticatedSignedWrites'] if json.get('authenticatedSignedWrites') is not None else None,
            extended_properties=json['extendedProperties'] if json.get('extendedProperties') is not None else None,
        )


def enable(
        state: CentralState,
        le_supported: bool,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Enable the BluetoothEmulation domain.

    :param state: State of the simulated central.
    :param le_supported: If the simulated central supports low-energy.
    '''
    params: T_JSON_DICT = {}
    params['state'] = state.to_json()
    params['leSupported'] = le_supported
    yield {'method': 'BluetoothEmulation.enable', 'params': params}


def set_simulated_central_state(
        state: CentralState,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Set the state of the simulated central.

    :param state: State of the simulated central.
    '''
    params: T_JSON_DICT = {}
    params['state'] = state.to_json()
    yield {'method': 'BluetoothEmulation.setSimulatedCentralState', 'params': params}


def disable() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Disable the BluetoothEmulation domain.'''
    yield {'method': 'BluetoothEmulation.disable'}


def simulate_preconnected_peripheral(
        address: str,
        name: str,
        manufacturer_data: list[ManufacturerData],
        known_service_uuids: list[str],
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Simulates a peripheral with |address|, |name| and |knownServiceUuids|
    that has already been connected to the system.

    :param address:
    :param name:
    :param manufacturer_data:
    :param known_service_uuids:
    '''
    params: T_JSON_DICT = {}
    params['address'] = address
    params['name'] = name
    params['manufacturerData'] = [i.to_json() for i in manufacturer_data]
    params['knownServiceUuids'] = list(known_service_uuids)
    yield {'method': 'BluetoothEmulation.simulatePreconnectedPeripheral', 'params': params}


def simulate_advertisement(entry: ScanEntry) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Simulates an advertisement packet described in |entry| being received by
    the central.

    :param entry:
    '''
    params: T_JSON_DICT = {}
    params['entry'] = entry.to_json()
    yield {'method': 'BluetoothEmulation.simulateAdvertisement', 'params': params}


def simulate_gatt_operation_response(
        address: str,
        type: GATTOperationType,
        code: int,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Simulates the response code from the peripheral with |address| for a
    GATT operation of |type|. The |code| value follows the HCI Error Codes from
    Bluetooth Core Specification Vol 2 Part D 1.3 List Of Error Codes.

    :param address:
    :param type:
    :param code:
    '''
    params: T_JSON_DICT = {}
    params['address'] = address
    params['type'] = type.to_json()
    params['code'] = code
    yield {'method': 'BluetoothEmulation.simulateGATTOperationResponse', 'params': params}


def simulate_characteristic_operation_response(
        characteristic_id: str,
        type: CharacteristicOperationType,
        code: int,
        data: str | None = None,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Simulates the response from the characteristic with |characteristicId| for a
    characteristic operation of |type|. The |code| value follows the Error
    Codes from Bluetooth Core Specification Vol 3 Part F 3.4.1.1 Error Response.
    The |data| is expected to exist when simulating a successful read operation
    response.

    :param characteristic_id:
    :param type:
    :param code:
    :param data: *(Optional)*
    '''
    params: T_JSON_DICT = {}
    params['characteristicId'] = characteristic_id
    params['type'] = type.to_json()
    params['code'] = code
    if data is not None:
        params['data'] = data
    yield {'method': 'BluetoothEmulation.simulateCharacteristicOperationResponse', 'params': params}


def simulate_descriptor_operation_response(
        descriptor_id: str,
        type: DescriptorOperationType,
        code: int,
        data: str | None = None,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Simulates the response from the descriptor with |descriptorId| for a
    descriptor operation of |type|. The |code| value follows the Error
    Codes from Bluetooth Core Specification Vol 3 Part F 3.4.1.1 Error Response.
    The |data| is expected to exist when simulating a successful read operation
    response.

    :param descriptor_id:
    :param type:
    :param code:
    :param data: *(Optional)*
    '''
    params: T_JSON_DICT = {}
    params['descriptorId'] = descriptor_id
    params['type'] = type.to_json()
    params['code'] = code
    if data is not None:
        params['data'] = data
    yield {'method': 'BluetoothEmulation.simulateDescriptorOperationResponse', 'params': params}


def add_service(address: str, service_uuid: str) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, str]:
    '''Adds a service with |serviceUuid| to the peripheral with |address|.

    :param address:
    :param service_uuid:
    :returns: An identifier that uniquely represents this service.
    '''
    params: T_JSON_DICT = {}
    params['address'] = address
    params['serviceUuid'] = service_uuid
    json = yield {'method': 'BluetoothEmulation.addService', 'params': params}
    return json['serviceId']


def remove_service(service_id: str) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Removes the service respresented by |serviceId| from the simulated central.

    :param service_id:
    '''
    params: T_JSON_DICT = {}
    params['serviceId'] = service_id
    yield {'method': 'BluetoothEmulation.removeService', 'params': params}


def add_characteristic(
        service_id: str,
        characteristic_uuid: str,
        properties: CharacteristicProperties,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, str]:
    '''Adds a characteristic with |characteristicUuid| and |properties| to the
    service represented by |serviceId|.

    :param service_id:
    :param characteristic_uuid:
    :param properties:
    :returns: An identifier that uniquely represents this characteristic.
    '''
    params: T_JSON_DICT = {}
    params['serviceId'] = service_id
    params['characteristicUuid'] = characteristic_uuid
    params['properties'] = properties.to_json()
    json = yield {'method': 'BluetoothEmulation.addCharacteristic', 'params': params}
    return json['characteristicId']


def remove_characteristic(
        characteristic_id: str,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Removes the characteristic respresented by |characteristicId| from the
    simulated central.

    :param characteristic_id:
    '''
    params: T_JSON_DICT = {}
    params['characteristicId'] = characteristic_id
    yield {'method': 'BluetoothEmulation.removeCharacteristic', 'params': params}


def add_descriptor(
        characteristic_id: str,
        descriptor_uuid: str,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, str]:
    '''Adds a descriptor with |descriptorUuid| to the characteristic respresented
    by |characteristicId|.

    :param characteristic_id:
    :param descriptor_uuid:
    :returns: An identifier that uniquely represents this descriptor.
    '''
    params: T_JSON_DICT = {}
    params['characteristicId'] = characteristic_id
    params['descriptorUuid'] = descriptor_uuid
    json = yield {'method': 'BluetoothEmulation.addDescriptor', 'params': params}
    return json['descriptorId']


def remove_descriptor(descriptor_id: str) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Removes the descriptor with |descriptorId| from the simulated central.

    :param descriptor_id:
    '''
    params: T_JSON_DICT = {}
    params['descriptorId'] = descriptor_id
    yield {'method': 'BluetoothEmulation.removeDescriptor', 'params': params}


def simulate_gatt_disconnection(address: str) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Simulates a GATT disconnection from the peripheral with |address|.

    :param address:
    '''
    params: T_JSON_DICT = {}
    params['address'] = address
    yield {'method': 'BluetoothEmulation.simulateGATTDisconnection', 'params': params}


@event_class('BluetoothEmulation.gattOperationReceived')
@dataclass(slots=True)
class GattOperationReceived:
    '''Event for when a GATT operation of |type| to the peripheral with |address|
    happened.
    '''
    address: str
    type: GATTOperationType

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> GattOperationReceived:
        return cls(
            address=json['address'],
            type=GATTOperationType.from_json(json['type']),
        )


@event_class('BluetoothEmulation.characteristicOperationReceived')
@dataclass(slots=True)
class CharacteristicOperationReceived:
    '''Event for when a characteristic operation of |type| to the characteristic
    respresented by |characteristicId| happened. |data| and |writeType| is
    expected to exist when |type| is write.
    '''
    characteristic_id: str
    type: CharacteristicOperationType
    #: *(Optional)*
    data: str | None = None
    #: *(Optional)*
    write_type: CharacteristicWriteType | None = None

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> CharacteristicOperationReceived:
        return cls(
            characteristic_id=json['characteristicId'],
            type=CharacteristicOperationType.from_json(json['type']),
            data=json['data'] if json.get('data') is not None else None,
            write_type=CharacteristicWriteType.from_json(json['writeType']) if json.get('writeType') is not None else None,
        )


@event_class('BluetoothEmulation.descriptorOperationReceived')
@dataclass(slots=True)
class DescriptorOperationReceived:
    '''Event for when a descriptor operation of |type| to the descriptor
    respresented by |descriptorId| happened. |data| is expected to exist when
    |type| is write.
    '''
    descriptor_id: str
    type: DescriptorOperationType
    #: *(Optional)*
    data: str | None = None

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> DescriptorOperationReceived:
        return cls(
            descriptor_id=json['descriptorId'],
            type=DescriptorOperationType.from_json(json['type']),
            data=json['data'] if json.get('data') is not None else None,
        )
