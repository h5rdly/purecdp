# DO NOT EDIT — generated by purecdp._generator from EventBreakpoints (devtools-protocol 3bd8f01934b5)
'''CDP domain: EventBreakpoints.

EventBreakpoints permits setting JavaScript breakpoints on operations and events
occurring in native code invoked from JavaScript. Once breakpoint is hit, it is
reported through Debugger domain, similarly to regular breakpoints being hit.

**EXPERIMENTAL**
'''

from __future__ import annotations

import enum
import typing
from dataclasses import dataclass

from .._shared import T_JSON_DICT, event_class


def set_instrumentation_breakpoint(
        event_name: str,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Sets breakpoint on particular native event.

    :param event_name: Instrumentation name to stop on.
    '''
    params: T_JSON_DICT = {}
    params['eventName'] = event_name
    yield {'method': 'EventBreakpoints.setInstrumentationBreakpoint', 'params': params}


def remove_instrumentation_breakpoint(
        event_name: str,
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Removes breakpoint on particular native event.

    :param event_name: Instrumentation name to stop on.
    '''
    params: T_JSON_DICT = {}
    params['eventName'] = event_name
    yield {'method': 'EventBreakpoints.removeInstrumentationBreakpoint', 'params': params}


def disable() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Removes all breakpoints'''
    yield {'method': 'EventBreakpoints.disable'}
