# DO NOT EDIT — generated by purecdp._generator from Log (devtools-protocol 3bd8f01934b5)
'''CDP domain: Log.

Provides access to log entries.
'''

from __future__ import annotations

import enum
import typing
from dataclasses import dataclass

from .._shared import T_JSON_DICT, event_class

from . import network, runtime


@dataclass(slots=True)
class LogEntry:
    '''Log entry.'''
    #: Log entry source. Allowed values: xml, javascript, network, storage, appcache, rendering, security, deprecation, worker, violation, intervention, recommendation, other
    source: str
    #: Log entry severity. Allowed values: verbose, info, warning, error
    level: str
    #: Logged text.
    text: str
    #: Timestamp when this entry was added.
    timestamp: runtime.Timestamp
    #: *(Optional)* Allowed values: cors
    category: str | None = None
    #: *(Optional)* URL of the resource if known.
    url: str | None = None
    #: *(Optional)* Line number in the resource.
    line_number: int | None = None
    #: *(Optional)* JavaScript stack trace.
    stack_trace: runtime.StackTrace | None = None
    #: *(Optional)* Identifier of the network request associated with this entry.
    network_request_id: network.RequestId | None = None
    #: *(Optional)* Identifier of the worker associated with this entry.
    worker_id: str | None = None
    #: *(Optional)* Call arguments.
    args: list[runtime.RemoteObject] | None = None

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['source'] = self.source
        json['level'] = self.level
        json['text'] = self.text
        json['timestamp'] = self.timestamp.to_json()
        if self.category is not None:
            json['category'] = self.category
        if self.url is not None:
            json['url'] = self.url
        if self.line_number is not None:
            json['lineNumber'] = self.line_number
        if self.stack_trace is not None:
            json['stackTrace'] = self.stack_trace.to_json()
        if self.network_request_id is not None:
            json['networkRequestId'] = self.network_request_id.to_json()
        if self.worker_id is not None:
            json['workerId'] = self.worker_id
        if self.args is not None:
            json['args'] = [i.to_json() for i in self.args]
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> LogEntry:
        return cls(
            source=json['source'],
            level=json['level'],
            text=json['text'],
            timestamp=runtime.Timestamp.from_json(json['timestamp']),
            category=json['category'] if json.get('category') is not None else None,
            url=json['url'] if json.get('url') is not None else None,
            line_number=json['lineNumber'] if json.get('lineNumber') is not None else None,
            stack_trace=runtime.StackTrace.from_json(json['stackTrace']) if json.get('stackTrace') is not None else None,
            network_request_id=network.RequestId.from_json(json['networkRequestId']) if json.get('networkRequestId') is not None else None,
            worker_id=json['workerId'] if json.get('workerId') is not None else None,
            args=[runtime.RemoteObject.from_json(i) for i in json['args']] if json.get('args') is not None else None,
        )


@dataclass(slots=True)
class ViolationSetting:
    '''Violation configuration setting.'''
    #: Violation type. Allowed values: longTask, longLayout, blockedEvent, blockedParser, discouragedAPIUse, handler, recurringHandler
    name: str
    #: Time threshold to trigger upon.
    threshold: float

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['name'] = self.name
        json['threshold'] = self.threshold
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> ViolationSetting:
        return cls(
            name=json['name'],
            threshold=float(json['threshold']),
        )


def clear() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Clears the log.'''
    yield {'method': 'Log.clear'}


def disable() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Disables log domain, prevents further log entries from being reported to the client.'''
    yield {'method': 'Log.disable'}


def enable() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Enables log domain, sends the entries collected so far to the client by means of the
    `entryAdded` notification.
    '''
    yield {'method': 'Log.enable'}


def start_violations_report(
        config: list[ViolationSetting],
) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''start violation reporting.

    :param config: Configuration for violations.
    '''
    params: T_JSON_DICT = {}
    params['config'] = [i.to_json() for i in config]
    yield {'method': 'Log.startViolationsReport', 'params': params}


def stop_violations_report() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Stop violation reporting.'''
    yield {'method': 'Log.stopViolationsReport'}


@event_class('Log.entryAdded')
@dataclass(slots=True)
class EntryAdded:
    '''Issued when new message was logged.'''
    #: The entry.
    entry: LogEntry

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> EntryAdded:
        return cls(
            entry=LogEntry.from_json(json['entry']),
        )
