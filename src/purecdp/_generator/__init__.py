'''Code generator: devtools-protocol JSON specs -> purecdp.protocol modules.

Dev-time tool only - not imported by the runtime library. Run with:

    python -m purecdp._generator [--spec DIR] [--out DIR]

CDP bindings are generated from the Chrome DevTools Protocol JSON (BSD-3-Clause, 
© Google), via the hyperiongray python-chrome-devtools-protocol (MIT) convention"

'''
