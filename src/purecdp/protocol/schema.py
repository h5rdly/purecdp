# DO NOT EDIT — generated by purecdp._generator from Schema (devtools-protocol 3bd8f01934b5)
'''CDP domain: Schema.

This domain is deprecated.

**DEPRECATED**
'''

from __future__ import annotations

import enum
import typing
from dataclasses import dataclass

from .._shared import T_JSON_DICT, event_class


@dataclass(slots=True)
class Domain:
    '''Description of the protocol domain.'''
    #: Domain name.
    name: str
    #: Domain version.
    version: str

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['name'] = self.name
        json['version'] = self.version
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> Domain:
        return cls(
            name=json['name'],
            version=json['version'],
        )


def get_domains() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, list[Domain]]:
    '''Returns supported domains.

    :returns: List of supported domains.
    '''
    json = yield {'method': 'Schema.getDomains'}
    return [Domain.from_json(i) for i in json['domains']]
