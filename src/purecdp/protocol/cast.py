# DO NOT EDIT — generated by purecdp._generator from Cast (devtools-protocol 3bd8f01934b5)
'''CDP domain: Cast.

A domain for interacting with Cast, Presentation API, and Remote Playback API
functionalities.

**EXPERIMENTAL**
'''

from __future__ import annotations

import enum
import typing
from dataclasses import dataclass

from .._shared import T_JSON_DICT, event_class


@dataclass(slots=True)
class Sink:
    name: str
    id: str
    #: *(Optional)* Text describing the current session. Present only if there is an active session on the sink.
    session: str | None = None

    def to_json(self) -> T_JSON_DICT:
        json: T_JSON_DICT = {}
        json['name'] = self.name
        json['id'] = self.id
        if self.session is not None:
            json['session'] = self.session
        return json

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> Sink:
        return cls(
            name=json['name'],
            id=json['id'],
            session=json['session'] if json.get('session') is not None else None,
        )


def enable(presentation_url: str | None = None) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Starts observing for sinks that can be used for tab mirroring, and if set,
    sinks compatible with |presentationUrl| as well. When sinks are found, a
    |sinksUpdated| event is fired.
    Also starts observing for issue messages. When an issue is added or removed,
    an |issueUpdated| event is fired.

    :param presentation_url: *(Optional)*
    '''
    params: T_JSON_DICT = {}
    if presentation_url is not None:
        params['presentationUrl'] = presentation_url
    yield {'method': 'Cast.enable', 'params': params}


def disable() -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Stops observing for sinks and issues.'''
    yield {'method': 'Cast.disable'}


def set_sink_to_use(sink_name: str) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Sets a sink to be used when the web page requests the browser to choose a
    sink via Presentation API, Remote Playback API, or Cast SDK.

    :param sink_name:
    '''
    params: T_JSON_DICT = {}
    params['sinkName'] = sink_name
    yield {'method': 'Cast.setSinkToUse', 'params': params}


def start_desktop_mirroring(sink_name: str) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Starts mirroring the desktop to the sink.

    :param sink_name:
    '''
    params: T_JSON_DICT = {}
    params['sinkName'] = sink_name
    yield {'method': 'Cast.startDesktopMirroring', 'params': params}


def start_tab_mirroring(sink_name: str) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Starts mirroring the tab to the sink.

    :param sink_name:
    '''
    params: T_JSON_DICT = {}
    params['sinkName'] = sink_name
    yield {'method': 'Cast.startTabMirroring', 'params': params}


def stop_casting(sink_name: str) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Stops the active Cast session on the sink.

    :param sink_name:
    '''
    params: T_JSON_DICT = {}
    params['sinkName'] = sink_name
    yield {'method': 'Cast.stopCasting', 'params': params}


@event_class('Cast.sinksUpdated')
@dataclass(slots=True)
class SinksUpdated:
    '''This is fired whenever the list of available sinks changes. A sink is a
    device or a software surface that you can cast to.
    '''
    sinks: list[Sink]

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> SinksUpdated:
        return cls(
            sinks=[Sink.from_json(i) for i in json['sinks']],
        )


@event_class('Cast.issueUpdated')
@dataclass(slots=True)
class IssueUpdated:
    '''This is fired whenever the outstanding issue/error message changes.
    |issueMessage| is empty if there is no issue.
    '''
    issue_message: str

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> IssueUpdated:
        return cls(
            issue_message=json['issueMessage'],
        )
