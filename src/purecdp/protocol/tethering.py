# DO NOT EDIT — generated by purecdp._generator from Tethering (devtools-protocol 3bd8f01934b5)
'''CDP domain: Tethering.

The Tethering domain defines methods and events for browser port binding.

**EXPERIMENTAL**
'''

from __future__ import annotations

import enum
import typing
from dataclasses import dataclass

from .._shared import T_JSON_DICT, event_class


def bind(port: int) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Request browser port binding.

    :param port: Port number to bind.
    '''
    params: T_JSON_DICT = {}
    params['port'] = port
    yield {'method': 'Tethering.bind', 'params': params}


def unbind(port: int) -> typing.Generator[T_JSON_DICT, T_JSON_DICT, None]:
    '''Request browser port unbinding.

    :param port: Port number to unbind.
    '''
    params: T_JSON_DICT = {}
    params['port'] = port
    yield {'method': 'Tethering.unbind', 'params': params}


@event_class('Tethering.accepted')
@dataclass(slots=True)
class Accepted:
    '''Informs that port was successfully bound and got a specified connection id.'''
    #: Port number that was successfully bound.
    port: int
    #: Connection id to be used.
    connection_id: str

    @classmethod
    def from_json(cls, json: T_JSON_DICT) -> Accepted:
        return cls(
            port=json['port'],
            connection_id=json['connectionId'],
        )
